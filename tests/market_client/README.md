# market_client tests

Nothing here yet — see `tests/market_server/README.md` for the sibling
suite that does exist. This directory is a placeholder so `tests/`
mirrors the repo root's `market_server`/`market_client` split
consistently, ready for whenever tests get added here.
