# git_repo/tests/market_client
#
# No tests here yet. This placeholder exists so the tests/ tree mirrors
# the repo root's market_server/market_client split from day one,
# instead of that structure only appearing once market_client actually
# gets test coverage. Add a real CMakeLists.txt (and whatever
# subdirectories market_server/'s tests/market_server/ needed) here when
# that happens.
